spring.application.name=AuroraApp
server.port=8080

# Qual profile será ativado (dev ou prod)
spring.profiles.active=@activated.properties@

# ---- Valores globais usados em todos os perfis ----

# Chave da OpenAI (vem do ambiente)
openai.api.key=${OPENAI_API_KEY:}

# JWT
jwt.secret=${JWT_SECRET:change-me}
jwt.expiration-ms=86400000
